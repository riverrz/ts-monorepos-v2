{
  "name": "@seeds/models",
  "version": "0.0.0",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.28.0",
    "typescript": "~5.8.3",
    "vitest": "^3.2.3"
  },
  "main": "dist/index.js",
  "repository": "https://github.com/mike-north/ts-monorepos-v2/tree/main/packages/models",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.build.json --watch --preserveWatchOutput",
    "lint": "eslint .",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui",
    "test:watch": "vitest"
  },
  "type": "module",
  "types": "dist/index.d.ts"
}
